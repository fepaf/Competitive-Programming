#include<stdio.h>
#include<string.h>
#define MAX (1<<10)
#define MOD 1000000007 

int n, st[MAX], r[MAX], i;
char s[11];

int main(){
    while (scanf("%d",&n)!=EOF){
        while (n--){
            scanf("%s",s);
            if (s[0]=='p' && ())
            for (i=0; s[i]!='_'; ++i);
            printf("%s\n",s);
        }
    }
    return 0;
}