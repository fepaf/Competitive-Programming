#include<bits/stdc++.h>

using namespace std;

int x, y, n, i; // -2**31 <= x < 2**31 ~ 10a9
float a, b; // 6 casas de precisao
char c; 
string s, r;

int main(){
    cin >> n;
    i = 0;
    while (i<n){
        cout << i << endl;
        ++i;
    }
    return 0;
}