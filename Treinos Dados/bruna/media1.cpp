#include<bits/stdc++.h>

using namespace std;

double a, b;

int main(){
    cin >> a >> b;
    cout << fixed << setprecision(5);
    cout << "MEDIA = " << (a*3.5+b*7.5)/(3.5+7.5) << endl;
    return 0;
}