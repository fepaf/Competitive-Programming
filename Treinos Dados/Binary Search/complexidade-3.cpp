#include<bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0); cin.tie(0);

using namespace std;

int n, a[i];

int main(){_
    cin >> n;
    for (int i=0; i<n; ++i){
        cin >> a[i];
        a[i] *= 2;
    }
    return 0;
}

// complexdiade de tempo de execução é O(2*n+7)